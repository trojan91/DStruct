#include<stdio.h>
#include"BIT_functions.h"

int main()
{
    char *grey_number;
    int size_num,num;

  /*  printf("Enter the size of length grey code\n");

    scanf("%d",&size_num);

    grey_number=(char *)malloc(sizeof(char) * size_num);

    printf("enter the grey number\n");
    scanf("%s",grey_number);
    grey_to_binary(grey_number);
    binary_to_grey(grey_number);

    free(grey_number);


    printf("enter the number\n");
    scanf("%d",&num);

    only_set_bit(num);


   turn_of_kth_bit(15,1);

      swap_nibble(1);

      */
      sparse_num(4);
    return 0;

}
